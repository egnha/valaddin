context("Strictly")

test_that("function unchanged if no checks given", {})

test_that("function environment is preserved", {})

test_that("function attributes are preserved", {})

test_that("function signature is preserved", {})

test_that("'...' checks are combined with .checklist", {})

test_that("adding checks preserves existing checks", {})

test_that("checks can be removed", {})

test_that(".warn_message = FALSE always removes missing argument check", {})

test_that(".warn_message = FALSE always adds missing argument check", {})

test_that("nonstrictly() returns original function, modulo class", {})

test_that("error is raised if .f is not a closure", {})

test_that("error is raised if .warning_message is not NULL or logical", {})

test_that("error is raised if checks form invalid checklist", {})
context("Removal of checks")

test_that("checks can be removed by numeric subsetter", {})

test_that("checks can be removed by logical subsetter", {})

test_that("error is raised if function not a strict closure", {})

test_that("error is raised if subsetter is out of range", {})

test_that("error is raised if subsetter is not numeric or logical", {})

test_that("attributes are preserved", {})

test_that("missing-argument-check behavior is preserved", {})
